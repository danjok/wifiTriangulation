<UserControl x:Class="WhereAmI.views.ActionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Loaded="OnControlLoaded">
    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="4*" />
        </Grid.ColumnDefinitions>
        <!-- 
        ListView: ItemsSource is assigned in the .cs file
        It is possible to describe a presentation template
        -->
        <ListView Grid.Column="0" Margin="10" Name="actionsViewData" DisplayMemberPath="Name" SelectionMode="Single"></ListView>
        <!-- GridSplitter to resize column -->
        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" />
        <StackPanel Name="actionViewDetail" Grid.Column="2" Margin="10,0,0,0" Visibility="Visible">
            <!-- 
            Note: this is the detail element 
            The binding is from data of another viewer element (ElementName) and precisely
            the element bound is given by the property SelectedItem
            -->
            <TextBox Name="ActionName" Text="{Binding ElementName=actionsViewData, Path=SelectedItem.Name}" FontSize="30" HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap" IsReadOnly="True"></TextBox>
            <Label Content="Command" FontSize="15"></Label>
            <TextBlock Text="{Binding ElementName=actionsViewData, Path=SelectedItem.Command}" FontSize="15" HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap"></TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
